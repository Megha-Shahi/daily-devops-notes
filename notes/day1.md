# Day 1
	• --soft: keeps changes staged
	• --mixed: keeps changes unstaged in working directory
      --hard: deletes changes entirely
	• --mixed: keeps changes unstaged in working directory
	• --mixed: keeps changes unstaged in working directory
	• --mixed: keeps changes unstaged in working directory
	• --mixed: keeps changes unstaged in working directory
What does @reboot do in crontab?
Answer: Runs the specified command once at system startup.

